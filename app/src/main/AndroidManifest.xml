<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.slf.moviescatalog">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Challenge5">
        <activity
            android:name="com.slf.moviescatalog.ui.UserProfileActivity"
            android:exported="false" />
        <activity
            android:name="com.slf.moviescatalog.ui.LoginActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.slf.moviescatalog.ui.MovieDetailsActivity" />
        <activity android:name="com.slf.moviescatalog.ui.MainActivity"
            android:exported="false" >

        </activity>
    </application>

</manifest>